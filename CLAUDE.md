# CLAUDE.md

## Agent skills

### Issue tracker

Issues and specs live as GitHub issues in `cosmapatricia/talk-to-your-data`, managed with the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

The five canonical triage roles, each mapped to its identically-named label. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context layout — one `CONTEXT.md` plus `docs/adr/` at the repo root. See `docs/agents/domain.md`.